# Eyestar Custom Driver
We do not have adequate information on EPS functionality yet to be able to fully map this out.

We could reasonably expect that we want the API to be simply `eyestar_send(data, len)` and `eyestar_receive(buffer)`

